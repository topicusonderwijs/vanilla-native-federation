[[Go back](./introduction.md)]

# Plugins

Plugins provide custom behavior for the orchestrator e.g. a `ConsoleLogger` instead of the default `NoopLogger`. 

- **Logging:** Provides the ConsoleLogger
- **Storage:** Provides the SessionStorage and LocalStorage implementations. 